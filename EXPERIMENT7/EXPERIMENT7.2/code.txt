CREATE TABLE Orders (
    Order_ID NUMBER PRIMARY KEY,
    Customer_Name VARCHAR2(100),
    Amount NUMBER(10, 2)
);

INSERT INTO Orders VALUES (101, 'RIDAM', 50000.00);
INSERT INTO Orders VALUES (102, 'RISHABH', 1250.50);
INSERT INTO Orders VALUES (103, 'AKSHAY', 800.00);
INSERT INTO Orders VALUES (104, 'TARUN', 25000.00);
INSERT INTO Orders VALUES (105, 'DILSHAN', 10000.00);


DECLARE

    CURSOR c_orders IS
        SELECT Order_ID, Customer_Name, Amount
        FROM Orders;
BEGIN

    FOR rec IN c_orders LOOP
        IF rec.Amount > 10000 THEN
            DBMS_OUTPUT.PUT_LINE(
                'Order ID: ' || rec.Order_ID || 
                ' | Customer: ' || rec.Customer_Name || 
                ' | Amount:' || rec.Amount || 
                ' -> High Value'
            );

        END IF;
    END LOOP;
END;